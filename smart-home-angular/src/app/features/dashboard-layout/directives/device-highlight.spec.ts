import { DeviceHighlight } from './device-highlight';

describe('DeviceHighlight', () => {
  it('should create an instance', () => {
    const directive = new DeviceHighlight();
    expect(directive).toBeTruthy();
  });
});
