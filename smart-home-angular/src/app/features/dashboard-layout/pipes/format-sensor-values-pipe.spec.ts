import { FormatSensorValuesPipe } from './format-sensor-values-pipe';

describe('FormatSensorValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSensorValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
