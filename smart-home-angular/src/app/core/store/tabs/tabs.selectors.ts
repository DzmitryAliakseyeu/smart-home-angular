// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { TabState } from "./tabs.reducer";

// export const selectTabState = createFeatureSelector<TabState>('tab');

// export const isEditModeTabOpen = createSelector(selectTabState, (state)=> state.isEditTab)
// export const getTab = createSelector(selectTabState, (state) => state.tab);
